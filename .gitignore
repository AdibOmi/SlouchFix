.venv/
.venv-notebook/
__pycache__/
*.pyc
data/raw/*.csv
data/raw/hf_posture_images/
data/processed/*
data/models/*
data/history/*.csv
datasets/*
!data/raw/.gitkeep
!data/processed/.gitkeep
!data/models/.gitkeep
!data/history/.gitkeep
!datasets/.gitkeep

# Pose-model weights downloaded on demand by notebooks/posture_pipeline_ablation.py
# (self-contained/re-downloadable -- see the notebook's install/download cells)
*.caffemodel
*.task
yolov8n-pose.pt

# MoveNet ONNX weights for the live app's pose.py -- see its error message
# for the download source; not committed (binary, easily re-fetched)
slouchfix/assets/movenet_lightning.onnx

# Ablation-study paper, slides source, notebooks, and reports -- not part of the app
notebooks/
paper/
reports/

.claude/
